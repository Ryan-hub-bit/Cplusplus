#include <iostream> //include all of the content of the library

// declaration
void Log(const char *);

int main() // entry point of the project, so every project can just have one main function
{
    Log("Hello world");
    std::cin.get();
}